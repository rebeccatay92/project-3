<ul class="nav navbar-nav">

  <% if user_signed_in? %>
    <p>Currently logged in as <i><%= current_user.name %></i></p>
  <% end %>

  <span><%=link_to "Home", home_path%></span>
  <span><%=link_to "Analysis", analysis_path %></span>
  <span><%=link_to "Portfolio", portfolios_path %></span>
  <span><%=link_to "Social Trading", social_trading_index_path %></span>

  <% if user_signed_in? %>
    <span><%= link_to "Log Out ", destroy_user_session_path, method: :delete %></span>
  <% else %>
    <span><%=link_to "Log In", new_user_session_path %></span>
    <span><%=link_to "Register", new_user_registration_path%></span>
  <% end %>

</ul>
